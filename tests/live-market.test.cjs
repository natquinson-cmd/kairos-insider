const {test}=require('node:test'),assert=require('node:assert/strict');const {convergences}=require('../assets/clarity/live-market-model.js');
test('convergence counts people rather than repeated purchases',()=>{const rows=[{ticker:'T',date:'2026-09-01',type:'buy',insiderCik:'1',value:100},{ticker:'T',date:'2026-09-02',type:'buy',insiderCik:'1',value:200}];assert.equal(convergences(rows).length,0);rows.push({ticker:'T',date:'2026-09-03',type:'buy',insiderCik:'2',value:50});assert.equal(convergences(rows)[0].buyers,2);assert.equal(convergences(rows)[0].amount,350);});
test('convergence excludes unknown identities and purchases outside 30 days',()=>{const rows=[{ticker:'T',date:'2026-08-01',type:'buy',insider:'A',value:100},{ticker:'T',date:'2026-09-15',type:'buy',insider:'B',value:200},{ticker:'T',date:'2026-09-16',type:'buy',value:300}];assert.equal(convergences(rows).length,0);});
test('convergence keeps currencies separate',()=>{assert.equal(convergences([{ticker:'T',currency:'USD',date:'2026-09-01',type:'buy',insider:'A'},{ticker:'T',currency:'EUR',date:'2026-09-02',type:'buy',insider:'B'}]).length,0);});
